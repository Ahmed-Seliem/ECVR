@model IEnumerable<ECM.ReservationSystem.Models.DTOs.HotelTrips.HotelCityResponseDto>

@if (Model.Any())
{
    <div class="table-responsive">
        <table class="table table-striped table-hover datatable">
            <thead>
                <tr>
                    <th>الاسم</th>
                    <th>الاسم بالعربية</th>
                    <th>الوصف</th>
                    <th>عدد الفنادق</th>
                    <th>الحالة</th>
                    <th>تاريخ الإنشاء</th>
                    <th>الإجراءات</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var city in Model)
                {
                    <tr>
                        <td>@city.Name</td>
                        <td>@(string.IsNullOrWhiteSpace(city.NameAr) ? "-" : city.NameAr)</td>
                        <td>@(string.IsNullOrWhiteSpace(city.Description) ? "-" : city.Description)</td>
                        <td><span class="badge bg-info">@city.HotelsCount</span></td>
                        <td>
                            @if (city.IsActive)
                            {
                                <span class="badge bg-success">نشطة</span>
                            }
                            else
                            {
                                <span class="badge bg-secondary">غير نشطة</span>
                            }
                        </td>
                        <td>@city.CreatedAt.ToString("dd/MM/yyyy")</td>
                        <td>
                            <div class="btn-group">
                                <button type="button"
                                        class="btn btn-sm btn-outline-warning js-edit-hotelcity"
                                        data-id="@city.Id"
                                        data-name="@city.Name"
                                        data-namear="@city.NameAr"
                                        data-description="@city.Description"
                                        data-active="@city.IsActive">
                                    <i class="fas fa-edit"></i>
                                </button>
                                <button type="button"
                                        class="btn btn-sm btn-outline-danger js-delete-hotelcity"
                                        data-id="@city.Id"
                                        data-name="@city.Name">
                                    <i class="fas fa-trash"></i>
                                </button>
                            </div>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
}
else
{
    <div class="text-center py-5">
        <i class="fas fa-city fa-3x text-muted mb-3"></i>
        <h5 class="text-muted">لا توجد مدن مضافة</h5>
    </div>
}
